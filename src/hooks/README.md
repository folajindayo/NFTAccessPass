# Hooks

Custom React hooks for encapsulated logic.

## Available Hooks

- `useMint`: Handles the NFT minting process including API calls and state management.
- `useAccess`: Checks if a given address holds the NFT Access Pass.
- `useTranslation`: Provides access to i18n strings and locale switching.

