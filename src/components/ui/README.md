# UI Components

Reusable atomic components that form the foundation of the design system.

## Components

- `Button`: Standard button with variants and loading state.
- `Card`: Container component with variants.

