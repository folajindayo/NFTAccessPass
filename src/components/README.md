# Components Directory

This directory contains all React components used in the application.

## Structure

- `ui/`: Atomic, reusable UI components (atoms/molecules).
- `layout/`: Structural components for page layout.
- `seo/`: Components related to Search Engine Optimization.
- Root: Feature-specific components.

